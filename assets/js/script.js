const date = document.getElementById("date");
const midiDiv = document.getElementById("midiDiv");
const soirDiv = document.getElementById("soirDiv");

date.addEventListener('input', function() {
 // midiDiv.innerHTML('<div> Test </div>');
  console.log(date.value);
})

//function weekDay() {
  // implementer ici la fonction qui va trouver le jour de la semaine et 
  //en fonction du jour retouner midi oul soir ou midi et soir
//   date.inner
//   date.append(  )

// }

// addEventListener("change", weekDay);